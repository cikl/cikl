define(['settings'],
function (Settings) {
  return new Settings({
    elasticsearch: window.location.origin+"/es",
    default_route: '/dashboard/file/cikl.json',
    kibana_index: "kibana-int",

    panel_names: [
      'histogram',
      'map',
      'goal',
      'table',
      'filtering',
      'timepicker',
      'text',
      'hits',
      'column',
      'trends',
      'bettermap',
      'query',
      'terms',
      'stats',
      'sparklines'
    ]
  });
});
