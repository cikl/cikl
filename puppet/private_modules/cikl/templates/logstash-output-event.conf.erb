output { 
  elasticsearch {
    index   => "cikl-%{+YYYY.MM.dd}"
    cluster => "<%= scope.lookupvar('es_cluster_name') %>"
    template => "<%= scope.lookupvar('es_cikl_template') %>"
    template_name => "cikl"
    template_overwrite => true
    index_type => "%{type}"
  }
}
