output { 
  elasticsearch {
    index   => "cikl-%{+YYYY.MM.dd}"
    cluster => "<%= scope.lookupvar('cikl::elasticsearch::cluster_name') %>"
    template => "<%= scope.lookupvar('cikl::logstash::elasticsearch_template') %>"
    template_name => "cikl"
    template_overwrite => true
    index_type => "%{type}"
  }
}
