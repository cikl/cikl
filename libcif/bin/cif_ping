#!/usr/bin/env perl

use strict;
use warnings;

# fix lib paths, some may be relative
BEGIN {
    require File::Spec;
    my @libs = (
        "lib", 
        "local/lib",
        "../libcif/lib", # in case we're in -dev mode
    );
    my $bin_path;

    for my $lib (@libs) {
        unless ( File::Spec->file_name_is_absolute($lib) ) {
            unless ($bin_path) {
                if ( File::Spec->file_name_is_absolute(__FILE__) ) {
                    $bin_path = ( File::Spec->splitpath(__FILE__) )[1];
                }
                else {
                    require FindBin;
                    no warnings "once";
                    $bin_path = $FindBin::Bin;
                }
            }
            $lib = File::Spec->catfile( $bin_path, File::Spec->updir, $lib );
        }
        unshift @INC, $lib;
    }
}

use CIF::Client;
use Getopt::Std;
use CIF qw/debug/;

my %opts;
getopt('C:', \%opts);

our $debug       = ($opts{'d'}) ? 1 : 0;

# config opts
my $config      = $opts{'C'} || $ENV{'HOME'}.'/.cif';

die usage() if($opts{'h'});

sub usage {
    return <<EOF;
Usage: perl $0

Standard Options:
    -h  --help:             this message
    -C  --config:           specify cofiguration file, default: $config
    
EOF
}

my ($err,$cli) = CIF::Client->new({
    config          => $config,
});

if($err){
    print 'ERROR: '.$err."\n";
    exit(-1);
}
my $response = $cli->ping();
$cli->shutdown();


print "Got a response: " . $response->to_string() . "\n";
